# symptom
现象：
- 报什么错 / 输出什么异常结果
- 运行到第 2 条 seam 后常出现 shape mismatch 或 seam 指向无效列；即使不报错，结果也会出现明显异常拉扯。

触发条件：
- 哪张图
- original.png
- 哪个 target size
- 例如 target=(237,336) 的 width shrink

归类：
- 建模错误 / 代码错误 / 求解异常 / 结果解释不足
- 求解异常
